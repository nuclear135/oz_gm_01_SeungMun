using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
RewardResolver는씬/오브젝트에붙는MonoBehaviour컴포넌트다.
-이스크립트는프로젝트에서RewardResolver역할을한다.
-컴포넌트참조는Awake에서캐싱하고,null을가드한다.
-외부호출지점을명확히하고중복처리를막는다.
*/
public class RewardResolver : MonoBehaviour
{
    
    void Start()
    {
        
    }

   
    void Update()
    {
        
    }
}
